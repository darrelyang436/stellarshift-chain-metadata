# @stellarshift/chain-metadata

Chain ID and RPC metadata helpers for EVM interviews and internal tooling.

```bash
npm install github:darrelyang436/stellarshift-chain-metadata
```

```js
const { chainName, isMainnet } = require("@stellarshift/chain-metadata");
```